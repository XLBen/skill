---
description: Build and verify a goal continuously until its original outcomes are met
agent: build
---

Load the `mvp-delivery` skill in Build Mode for:

$ARGUMENTS

Load `i-have-adhd` for user-facing progress, acceptance-preview, delivery and
blocker output. Routing, delegation, concurrency and failure branches follow
`mvp-delivery/references/subagent-orchestration.md`; the controller owns
integration, engine gates and Finish With Evidence. Never stop merely because
the first slice passed.
