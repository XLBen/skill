---
description: Turn a goal or confirmed brief into a right-sized executable plan
agent: build
---

Load the `mvp-delivery` skill in Plan Mode for:

$ARGUMENTS

Load `i-have-adhd` for the plan preview, owner decision prompt and final handoff.
Planning is read-only for product code; any research seats follow
`mvp-delivery/references/subagent-orchestration.md` and never dispatch writers.
End with the single `/build` handoff and any pending owner confirmation; keep
the complete `ACCEPTANCE_HANDOFF` for PUA/reviewer.
