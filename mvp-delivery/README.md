# mvp-delivery

OpenCode skill behind the public `/plan`, `/build`, and `/resume` commands,
and the internal autonomous work and defect-repair methods for ordinary user
requests. It produces a runnable thin slice, verifies real behavior, and keeps
adding slices until the user's original goal is met.

Use `/grill` separately when the idea itself needs deep clarification.
