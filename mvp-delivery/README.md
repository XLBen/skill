# mvp-delivery

OpenCode skill behind the public `/plan`, `/build`, `/fix`, and `/resume`
commands. It produces a runnable thin slice quickly, verifies real behavior,
and keeps adding slices until the user's original goal is met.

Use `/grill` separately when the idea itself needs deep clarification.
