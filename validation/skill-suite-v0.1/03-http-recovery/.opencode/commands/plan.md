---
description: Compile a passed contract into a confirmed executable PLAN
model: zhipuai-coding-plan/glm-5.3
---

Load the `contract-review` skill and run its planning phase.
Validate the contract, its intake provenance, the Phase 0 result, and the
declared first-slice budget before compiling `docs/PLAN.md`. Compile only the
first thin end-to-end slice; planned later scope belongs in an SI.
